# Implementation Tasks

## 阶段 1：数据库与模型

- `T-001` 在 `schema.sql` 增加 RBAC 表结构（参考 `S-001~S-005`）
- `T-002` 在 `data.sql` 加入种子数据（参考 `S-006~S-010`）
- `T-003` 新增 DO/Mapper/DAO：`AdminUser`, `Role`, `Permission`

## 阶段 2：认证与会话

- `T-010` 新增登录/登出/当前用户接口（`A-001~A-003`）
- `T-011` 增加 Session 用户上下文封装
- `T-012` 密码校验改为 BCrypt

## 阶段 3：授权

- `T-020` 增加权限拦截器（或注解切面）
- `T-021` 给管理端接口标注权限点
- `T-022` 统一 401/403 异常响应

## 阶段 4：前端可见性控制

- `T-030` 管理端页面初始化时拉取当前权限
- `T-031` 按权限显示/隐藏菜单
- `T-032` 按权限禁用/隐藏按钮

## 阶段 5：角色分配能力

- `T-040` 实现管理用户查询接口（`A-010`）
- `T-041` 实现用户角色分配接口（`A-011`）

## 阶段 6：测试

- `T-050` 认证接口测试（登录成功/失败、登出）
- `T-051` 授权测试（未登录 401、越权 403、授权 200）
- `T-052` 关键权限点回归测试
## 执行状态

- `T-001` 已完成
- `T-002` 已完成
- `T-003` 已完成
- `T-010` 已完成
- `T-011` 已完成
- `T-012` 已完成（采用“登录时明文自动升级为 BCrypt 哈希”）
- `T-020` 已完成
- `T-021` 已完成（首批应用在用户角色分配相关接口）
- `T-022` 已完成
- `T-040` 已完成
- `T-041` 已完成
- `T-050` 已完成
- `T-051` 已完成
- `T-052` 部分完成（当前覆盖 RBAC 核心链路，业务权限点待后续业务接口补齐）
